{% extends 'FiThnitek/basebackFiThnitek.html.twig' %}
{% block content %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">All carsharing offers </h3>

                    <div class="card-tools">
                        <div  style="width: 1050px;">
                           <form method="post">
                            <input type="text" name="destinationrechercheback"  placeholder="Destination">
                            <input type="text" name="departrechercheback"  placeholder="Departure">
                            <input type="number" name="nbrplacerechercheback"  placeholder="Number">
                               <input type="date" name="daterechercheback"  >
                            <button type="submit" formaction="{{ path('fi_thnitek_recherchecovback') }}" class="btn btn-default"><i class="fas fa-search"></i></button>
                           </form>
                        </div>
                    </div>
                </div>
                <!-- /.card-header -->
                <div class="card-body table-responsive p-0">
                    <table class="table table-hover text-nowrap">
                        <thead>
                        <tr>
                            <th>Driver Name</th>
                            <th>User Number</th>
                            <th>Destination</th>
                            <th>Departure</th>
                            <th>Car</th>
                            <th>Date</th>
                            <th>Number of places</th>
                            <th>Price</th>
                            <th>Delete</th>
                            <th>Update</th>
                            <th>Reserve places</th>
                            <th>Reserve</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for i in cov %}
                        <tr>
                            <td><a href="{{ path('fi_thnitek_finduserback',{'id': i.idutilisateur.id}) }}">{{ i.idutilisateur.username }} {{ i.idutilisateur.prenom }}</a></td>
                            <td>{{ i.idutilisateur.tel }}</td>
                            <td>{{ i.destination  }}</td>
                            <td>{{ i.depart }}</td>
                            <td>{{ i.voiture }}</td>
                            <td>{{ i.date }}</td>
                            <td><center>{{i.nbrplaceo  }}</center></td>
                            <td>{{ i.prix }}</td>
                            <td>
                                <a class="btn btn-block btn-outline-danger" href="{{ path('fi_thnitek_supprimercovback',{'id': i.idoffrecovoiturage }) }}">Delete</a>

                            </td>
                            <td>
                                <a class="btn btn-block btn-outline-success" href="{{ path('fi_thnitek_modifierrcovback',{'id': i.idoffrecovoiturage }) }}">Update</a>

                            </td>
                            <form method="post">
                            <td>
                                <input type="number" name="nbrrback">
                            </td>

                                <td>
                                    <button type="submit" class="btn btn-block btn-outline-info" formaction="{{ path('fi_thnitek_resevcovback',{'id': i.idoffrecovoiturage }) }}">Reserv</button>
                                </td>
                            </form>
                        </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>


{% endblock %}
